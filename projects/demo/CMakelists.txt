set(ProjectName Demo)
project(${ProjectName} VERSION 1.0.0 LANGUAGES C CXX)

file(GLOB_RECURSE source_files
    **.cpp
    **.hpp
)

add_executable(${ProjectName} ${source_files})
set_target_properties(${ProjectName} PROPERTIES OUTPUT_NAME "Game")
set_target_properties(${ProjectName} PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_SOURCE_DIR}/bin")
target_include_directories(${ProjectName} PRIVATE "./")
target_link_libraries(${ProjectName} PRIVATE App)